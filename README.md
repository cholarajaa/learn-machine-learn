## Teaching machine to learn some classification

create a new virtual environment

* virtualenv mlenv # -- you can specify folder to keep the enviroment in specific folder

* pip install -r requirements.txt

